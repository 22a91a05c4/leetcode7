class Solution {
    public boolean stoneGame(int[] piles) {
        /*Arrays.sort(piles);
        int n = piles.length/2;
        int a=0,b=0;
        for(int i=0;i<n;i++)
        {
            a=a+piles[i];
        }
        for(int i=n;i<piles.length;i++)
        {
            b=b+piles[i];
        }
        return a<b;*/
        return true;
    }
}  